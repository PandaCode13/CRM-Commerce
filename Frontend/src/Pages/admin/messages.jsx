import "./css/dashboard.css";

const AdminMessages = () => {
  return (
    <main className="admin-dashboard">
      <h4>Messages reçus</h4>

      <section>
        <p>Aucun message pour le moment. Les messages seront reliés à l’API dès que le module sera disponible.</p>
      </section>
    </main>
  );
};

export default AdminMessages;
